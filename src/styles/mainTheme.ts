import colors from "./colors";
import sizes from "./sizes";

const mainTheme = {
  colorPrimary: colors.navyBlue,
  backgroundColor: colors.mainWhite,
  paddingHorizontal: sizes.body.paddingHorizontal,
};

export default mainTheme;
