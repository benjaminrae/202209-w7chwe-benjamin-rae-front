import { UiState } from "../../redux/features/uiSlice/types";

const mockUiState: UiState = {
  isLoading: false,
  isError: false,
  modalText: "",
  showModal: false,
};

export default mockUiState;
